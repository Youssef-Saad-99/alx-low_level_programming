#include "main.h"
#include "stdio.h"

/**
 * _puts - A function that prints a string, followed by a new line, to stdout
 *
 * @str: pointer takes a string input
*/

void _puts(char *str)
{
	printf("%s\n", str);
}
