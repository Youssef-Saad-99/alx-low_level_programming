#include "main.h"
#include <stdio.h>

/**
 * _puts_recursion - A function that prints a string, followed by a new line.
 *
 * @s: pointer input
*/

void _puts_recursion(char *s)
{
	printf("%s\n", s);
}
