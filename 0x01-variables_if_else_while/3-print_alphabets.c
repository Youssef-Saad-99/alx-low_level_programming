#include <stdio.h>

/**
 * main - Entry point
 * Description: print all alphabet in lowercase and uppercase
 * Return: 0 (Success)
*/

int main(void)
{
	char ch = 'a', ch1 = 'A';

	while (ch <= 'z' && ch1 <= 'Z')
	{
		putchar(ch);
		putchar(ch2);
		ch++;
		ch1++;
	}
	putchar('\n');
	return (0);
}
